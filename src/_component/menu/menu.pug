.menu
  if showBackButton
    a(href=showBackButton).menu__disc
      .icon__circle-left
  img.menu__title(src="../../asset/image/logo/logo1.jpg")
  //- h2.menu__title Profesional Mastic Applier
  input(type="checkbox" id="menu")
  label(for="menu").menu__disc
    .icon__menu
    .icon__cross
  .menu__list
    each item in data.menu
      a(
        href=`${baseUrl}${item.link}`
        title=item.label
      ).menu__item=item.label

